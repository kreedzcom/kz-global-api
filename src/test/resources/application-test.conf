database {
    url = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1"
    maximumPoolSize = 2
}

r2 {
    endpoint = "https://test.r2.example.com"
    accessKeyId = "test-key-id"
    secretAccessKey = "test-secret"
    bucket = "test-bucket"
}

admin {
    bearerKey = "test-admin-bearer-key"
}
